export enum ApiEndpoints {
  message = "/message",
  client = "/client",
  agent = "/agent",
  user = "/user",
  company = "/company",
  font = "/font",
  session = "/session",
  cssParam = "/css-param",
  testContext = "/test-context",
  storage = "/storage",
}
