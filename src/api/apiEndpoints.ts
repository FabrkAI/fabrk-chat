export enum ApiEndpoints {
  sms = "/sms",
  lead = "/lead",
  campaign = "/campaign",
  user = "/user",
  company = "/company",
  companyConfig = "/company-config",
  leadCampaign = "/lead-campaign",
  font = "/font",
  session = "/session",
  cssParam = "/css-param",
}
