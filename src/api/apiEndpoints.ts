export enum ApiEndpoints {
  sms = "/sms",
  lead = "/lead",
  agent = "/agent",
  user = "/user",
  company = "/company",
  font = "/font",
  session = "/session",
  cssParam = "/css-param",
  testContext = "/test-context",
  storage = "/storage",
}
